spring:
  datasource:
    url: jdbc:mysql://localhost:3306/amy_community_jpa?serverTimezone=UTC&useSSL=false
    username: root
    password: Amysm9022@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

server:
  port: 8080

  springdoc:
    swagger-ui:
      path: /swagger-ui.html
    api-docs:
      path: /api-docs